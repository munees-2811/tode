# AGPL-free minimal runtime — ONNX inference only (no torch, no ultralytics).
# Use this when you want to run tode with .onnx weights without pulling in the
# AGPL-licensed ultralytics package.
opencv-python>=4.8.0,<5.0.0
Pillow>=10.0.0
numpy>=1.24.0,<3.0.0
requests>=2.28.0
onnxruntime>=1.17.0
